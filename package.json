{
  "name": "smart-socket-server",  // Projenin adı (sunucu tarafı)
  "version": "1.0.0",            // Projenin sürüm numarası
  "type": "module",              // Node.js'in ES module (import/export) kullanacağını söyler
  "scripts": { 
    "start": "node server.js"    // "npm start" yazıldığında çalışacak komut
  },
  "dependencies": {              // Projenin ihtiyaç duyduğu paketler
    "cors": "^2.8.5",            // Tarayıcıdan gelen farklı domain isteklerine izin veren middleware
    "express": "^4.18.2",        // HTTP sunucusu oluşturmak için kullanılan web framework
    "ws": "^8.13.0"              // WebSocket desteği (canlı veri iletişimi)
  }
}
